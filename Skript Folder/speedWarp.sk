# WIP - assign a number to a /home to speed up going there
# <set | remove | (num)> <home name> <ID>
command /speedwarp <string> [<string>] [<integer>]:
	usage:
	trigger:
		if arg-1 is "set":
			
		else if arg-1 is "del":
			
		else if arg-1 parsed as a number is a number:
		